const path = require("path");

module.exports = {
  mode: "production",
  entry: "./index.js", // entry point of our project
  target: "node",
  module: {
    rules: [
      {
        test: /\.(js)$/,
        exclude: /(node_modules|bower_components)/,
        use: "babel-loader",
      },
    ],
  },

  resolve: {
    extensions: [".js"],
    fallback: {
      fs: false,
      events: false,
      path: false,
    },
  },
  externals: {
    pg: "pg",
    uuid: "uuid",
    "prompt-sync": "prompt-sync",
  },
  output: {
    // empty the output directory on every build
    clean: true,

    path: path.resolve(__dirname, "./dist"),
    filename: "index.js",
    libraryTarget: "umd",
    globalObject: "this",
    // libraryExport: 'default',
    library: "easy-postgresql",
  },
};
